<UserControl x:Class="TodoList.FullButtonTwoDataBindingsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TodoList"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Button>
        <DockPanel LastChildFill="False" Margin="5">
            <Label Content="{Binding Path=Label, Mode=OneWay}" DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="16" />
            <Label Content="{Binding Path=Count, Mode=OneWay}" DockPanel.Dock="Right" VerticalAlignment="Center" FontSize="12" Foreground="#FF767676"/>
        </DockPanel>
    </Button>
</UserControl>
